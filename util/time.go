package util

import (
	"goblog/model"
	"time"
)

func Totime(view *[]model.ViewJson) (err error) {
	for i := 0; i < len(*view); i++ {

	}
	return
}

func Time(view *model.ViewJson) (err error) {

	return
}

func TimeToString(time.Time) (s string) {

	return
}
