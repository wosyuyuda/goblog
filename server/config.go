package server

/* import (
	d "goblog/model"
)

func GetConfig(id string) *d.Config {
	db := d.GetDb
	c := new(d.Config)
	db.Where("id = ?", id).First(&c)
	//db.Where("id = ?", id).Find(&c)
	return c
} */
